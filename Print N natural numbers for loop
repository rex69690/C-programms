#include <stdio.h>

int main(void) {
  int  i , n;
  printf("Input upper limit: ");
  scanf("%d",&n);
  printf("Natural numbers from 1 to %d\n:");
  for (i=1; i<=n;i++)
    {
      printf("%d\n",i);
    }
}
